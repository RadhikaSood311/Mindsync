import React from 'react'
import '../App.css'

export default function StudyMode(){
  return (
    <main className="page-root">
      <section className="section">
        <div className="section-inner">
          <h1 className="section-title">Study Mode</h1>
          <p className="section-sub muted">This page intentionally does not render the shared feature modules — they live on the Home page only.</p>
        </div>
      </section>
    </main>
  )
}
